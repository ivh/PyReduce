import numpy as np

import clib._extract.lib as extactlib
from clib._extract import ffi



def extract(img, ycen, height, swath=64,
         osample=10, smooth_spec=0.0, smooth_slit=1.0 )

    return spec, err, slit, model

